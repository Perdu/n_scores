#!/usr/bin/env pythonw

import MySQLdb as mdb
import sys

user = ''
password = ''

if password == '' or user == '':
    print "Please set database user and password in config.py"
    sys.exit()

is_debug_activated = False
# set to "0.0.0.0" to make the app reachable from the internet
host = "127.0.0.1"
